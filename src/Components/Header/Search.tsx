import React from 'react';
import { Input } from 'antd';

const AntdInput = Input.Search;

const Search: React.FC<{}> = ({}) => {
  return (
    <AntdInput></AntdInput>
  );
};

export default Search;
