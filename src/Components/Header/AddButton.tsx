import React from 'react';
import { PlusOutlined } from '@ant-design/icons';

const AddButton: React.FC<{}> = ({}) => {
  return <PlusOutlined style={{marginRight: '20px'}}></PlusOutlined>;
};

export default AddButton;
